[setup]
runtime = "nodejs_20"

[install]
commands = [
  "npm install -g npm@9",  # Ensure npm is updated to version 9.x
  "npm install -g corepack@0.24.1 && corepack enable",
  "npm i"
]

[build]
commands = [
  "npm run build"
]

[start]
commands = [
  "npm run start"
]
